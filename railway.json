{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "corepack enable && corepack prepare pnpm@10.4.1 --activate && pnpm release:check && pnpm install --frozen-lockfile && pnpm build"
  },
  "deploy": {
    "preDeployCommand": "pnpm db:migrate && if [ \"${BOOTSTRAP_ADMIN_ON_DEPLOY:-false}\" = \"true\" ]; then pnpm admin:create; fi",
    "startCommand": "NODE_ENV=production node dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
